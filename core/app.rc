// Windows resource script: embeds the app icon into InternetDownloader.exe (shown in
// Explorer, the taskbar, and Alt-Tab) and makes it loadable at runtime by name via
// wxIcon(wxICON(IDI_ICON1)) in main_window.cpp / tray_icon.cpp.
IDI_ICON1 ICON "assets/icons/app.ico"
